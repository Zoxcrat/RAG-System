.env
__pycache__/
*.pyc
venv/
.venv/
.pytest_cache/
.DS_Store

# Large scanned source PDFs live in data/ but stay out of git to keep the repo
# and pushes lean. The Docker build still bakes whatever PDF is present locally.
data/*.pdf

# OCR cache derived from the PDF (regenerated by the extraction step). Large and
# reproducible, so it is kept out of git like the source PDF.
data/*ocr*.json

# Personal project notes / Claude guidance — kept local, not versioned.
CLAUDE.md


# Personal interview prep — kept local, not shared in the repo
docs/12-preguntas-entrevista.md
docs/COMO-TRABAJO-Y-LA-IA.md
