OPENAI_API_KEY=

DB_HOST=localhost
DB_PORT=5432
DB_NAME=rag_db
DB_USER=rag_user
DB_PASSWORD=rag_password

# Public demo: gate the whole app behind HTTP Basic auth (leave unset to keep it open).
# DEMO_USER=demo
# DEMO_PASSWORD=

# Optional tuning (sensible defaults are baked into src/config.py)
# OPENAI_MAX_RETRIES=3
# OPENAI_TIMEOUT=30
# EMBEDDING_MODEL=text-embedding-3-small
# LLM_MODEL=gpt-4o-mini
# RELEVANCE_THRESHOLD=0.5
# CHUNK_SIZE=500
# CHUNK_OVERLAP=100
